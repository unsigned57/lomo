<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:label="Lomo Native Smoke"
        android:supportsRtl="true">
        <activity
            android:name=".NativeSmokeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- DocumentsProvider requires exported + MANAGE_DOCUMENTS; same-UID smoke app may call itself. -->
        <provider
            android:name=".FeasibilityDocumentsProvider"
            android:authorities="com.lomo.nativesmoke.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
    </application>
</manifest>
