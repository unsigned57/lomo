# Feasibility dependency four-ABI **linked** evidence

Tooling crate `lomo-feasibility-device` retains rusqlite/pulldown-cmark/reqwest/git2 via live call paths (`candidate_link_markers` + SQLite/Markdown probes) in `liblomo_feasibility_device.so` for Android API 26 via cargo-ndk (`release-ci`).

**Proves:** constructor/version retention of candidate crates after LTO (volume selection).
**Does not prove:** full smart-HTTP push/rebase or HTTP stream matrices inside this SO (those remain host-fixture contracts).

Each `.so` is `strings`-checked for exact sentinels `LOMO_LINK_MARKER_GIT2_v1`, `LOMO_LINK_MARKER_REQWEST_RUSTLS_v1`, `LOMO_LINK_MARKER_SQLITE_v1`. Generic OpenSSL/aws-lc strings are **not** accepted.
Not packaged into production `app/jniLibs`.

| ABI | Result | `.so` bytes |
| --- | --- | --- |
| `arm64-v8a` | pass (ELF + exact LOMO_LINK_MARKER_*) | 11763688 |
| `armeabi-v7a` | pass (ELF + exact LOMO_LINK_MARKER_*) | 8338772 |
| `x86_64` | pass (ELF + exact LOMO_LINK_MARKER_*) | 13344968 |
| `x86` | pass (ELF + exact LOMO_LINK_MARKER_*) | 11014476 |

Host runtime: `cargo test -p lomo-feasibility-device --test device_bundle_contract` (SQLite+Markdown+exact markers).
Device process load of this SO is optional tooling; production ownership still stage-gated.

Generated by `lomo-xtask` `verify_feasibility_android_targets`.
