fun interface {{ interface_name }} {
    fun invoke({% for param in params %}{{ param.name }}: {{ param.kotlin_type }}{% if !loop.last %}, {% endif %}{% endfor %}){% if !is_void_return %}: {{ return_type }}{% endif %}
}
